# 네이버 스마트에디터 ONE & AI 원고 변환 영구 시스템 규칙 (NAVER_BOT_RULES.md)

이 문서는 모든 AI 모델 및 포스팅 봇 구동 시 100% 오류 재발을 방지하기 위해 영구 각인된 시스템 행동 규칙입니다.

---

## 1. Google Gemini API 모델 호출 규칙
- **허용 모델**: 오직 공식 검증된 표준 모델 `gemini-2.0-flash` 만을 사용한다.
- **CORS 및 404 차단**: 브라우저 직접 호출 대신 반드시 백엔드 컨트롤러(`/api/convert-text`)를 경유한다.
- **429 Rate Limit 처리**: 구글 무료 계정의 사용량 락 발생 시 오류 팝업을 띄우지 않고 60초 백오프 백그라운드 대기로 자동 완결한다.
- **OpenAI 듀얼 지원**: `sk-` 시작 키 입력 시 GPT-4o-mini 유료 엔진으로 0.1초 즉시 변환한다.

---

## 2. 네이버 스마트에디터 ONE 포스팅 RPA 봇 규칙
- **팝업 자동 소탕**: "작성 중인 글이 있습니다" 감지 시 0.5초 만에 `[취소]` 버튼을 무조건 클릭하여 새 글로 진입한다.
- **양식 완전 동기화 (샘플 224361192618)**:
  - 1열 5행 목차 표 (첫 행 배경색 `#ef8aaa`, 남색 볼드 `#2b2a73`)
  - 소제목 1~4번 (남색 `#2b2a73`, 폰트 크기 19px, 볼드, 분홍색 글자 배경 `#ffdce7`)
  - 본문/인용구 나눔스퀘어 폰트 & 가운데 정렬
- **포커스 클릭 및 저장**: 제목 입력 후 본문 영역 노드를 직접 마우스 클릭(`click()`)하여 커서를 확보한 뒤 4초 대기 후 `[임시저장]` 버튼을 누른다.
